[project]
name = "resume"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dynaconf>=3.3.2",
    "openai>=2.45.0",
    "pandas>=3.0.3",
]

[dependency-groups]
dev = [
    "pre-commit>=4.6.0",
]
